<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="contact"
            type="com.naposystems.pepito.entity.Contact" />

        <variable
            name="viewModel"
            type="com.naposystems.pepito.utility.sharedViewModels.userDisplayFormat.UserDisplayFormatShareViewModel" />

    </data>

    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:id="@+id/initial_menu"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:elevation="@dimen/elevation"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:padding="0dp"
            tools:background="@color/colorPrimary">

            <androidx.appcompat.widget.AppCompatImageButton
                android:id="@+id/button_back"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/normal_margin"
                android:layout_marginEnd="@dimen/small_margin"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:tint="?attr/attrBackgroundTintIconToolbar"
                app:srcCompat="@drawable/ic_arrow_back_white" />

            <androidx.appcompat.widget.LinearLayoutCompat
                android:id="@+id/container_description_user"
                android:layout_width="wrap_content"
                android:layout_height="?android:attr/actionBarSize"
                android:orientation="horizontal">

                <ImageView
                    android:id="@+id/imageView_avatar"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:layout_gravity="center_vertical"
                    android:background="@drawable/border_avatar_contact"
                    app:avatarActionBar="@{contact}"
                    tools:src="@drawable/ic_spanish_flag" />

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_vertical"
                    android:layout_marginStart="@dimen/small_margin"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/textView_user_display_name"
                        style="@style/SmallBodyTextView.WhiteBold"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:ellipsize="end"
                        android:lines="1"
                        android:textColor="?attr/attrTextColorNameConversation"
                        app:contactIdNameFormat="@{contact.id}"
                        app:nameActionBar="@{contact}"
                        app:nameFormatContact="@{viewModel.userDisplayFormat}"
                        tools:text="Giussep Ricardo Cachaya" />

                    <TextView
                        android:id="@+id/textView_user_nickname"
                        style="@style/SmallBodyTextView.White"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:ellipsize="end"
                        android:lines="1"
                        android:textColor="?attr/attrTextColorNicknameConversation"
                        app:contactIdNickNameFormat="@{contact.id}"
                        app:nickNameFormat="@{viewModel.userDisplayFormat}"
                        app:nicknameActionBar="@{contact}"
                        tools:text="\@giussepr" />
                </LinearLayout>

            </androidx.appcompat.widget.LinearLayoutCompat>

        </LinearLayout>

    </androidx.appcompat.widget.LinearLayoutCompat>


</layout>